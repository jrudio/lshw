const fs = require('fs');
const os = require('os');

const {
  getCPUs,
  getMemory,
  getStorageDevices,
  getNetworkInterfaces
} = require('./helpers');

(() => {
  // get relevant info

  // format into expected output

  // turn into JSON

  // write to disk as machinedata.json

})();